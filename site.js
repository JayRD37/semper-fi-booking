const header=document.querySelector('.site-header');const button=document.querySelector('.menu-button');const menu=document.querySelector('.mobile-menu');const overlay=document.querySelector('.menu-overlay');
const menuLinks=[...menu.querySelectorAll('a')];
const closeMenu=()=>{const wasOpen=menu.classList.contains('open');button.classList.remove('open');menu.classList.remove('open');overlay.classList.remove('open');button.setAttribute('aria-expanded','false');menu.setAttribute('aria-hidden','true');document.body.style.overflow='';if(wasOpen)button.focus()};
const toggleMenu=()=>{const open=!menu.classList.contains('open');button.classList.toggle('open',open);menu.classList.toggle('open',open);overlay.classList.toggle('open',open);button.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-hidden',String(!open));document.body.style.overflow=open?'hidden':'';if(open)menuLinks[0].focus()};
button.addEventListener('click',toggleMenu);overlay.addEventListener('click',closeMenu);menuLinks.forEach(link=>link.addEventListener('click',closeMenu));window.addEventListener('keydown',event=>{if(event.key==='Escape')closeMenu();if(event.key==='Tab'&&menu.classList.contains('open')){const first=menuLinks[0],last=menuLinks[menuLinks.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}}});window.addEventListener('scroll',()=>header.classList.toggle('is-scrolled',window.scrollY>24),{passive:true});
const form=document.querySelector('#booking-form');const result=document.querySelector('#inquiry-result');const summary=document.querySelector('#inquiry-summary');const copyButton=document.querySelector('#copy-inquiry');const copyStatus=document.querySelector('#copy-status');const clearButton=document.querySelector('.clear-draft');const draftKey='semperFiBookingDraft';
const artistField=document.createElement('div');artistField.className='field';artistField.innerHTML='<label for="inquiry-artist">Artist</label><select id="inquiry-artist" name="artist"><option value="">Choose an artist</option><option>Anthony Segovia</option><option>The Traynr Band</option><option>Briella Steiner</option><option>Not sure yet</option></select>';form.querySelector('#inquiry-type').closest('.field').before(artistField);const artistSelect=artistField.querySelector('select');
const labels={name:'Name',organization:'Organization / Venue',artist:'Artist',eventType:'Event Type',eventDate:'Event Date',city:'City',state:'State',capacity:'Estimated Capacity',budget:'Budget / Offer',details:'Additional Details'};
const saveDraft=()=>{const data=Object.fromEntries(new FormData(form));localStorage.setItem(draftKey,JSON.stringify(data))};
const loadDraft=()=>{try{const data=JSON.parse(localStorage.getItem(draftKey)||'{}');Object.entries(data).forEach(([key,value])=>{if(form.elements[key])form.elements[key].value=value})}catch{localStorage.removeItem(draftKey)}};
form.addEventListener('input',saveDraft);form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const data=Object.fromEntries(new FormData(form));summary.textContent=['SEMPER FI BOOKING INQUIRY','',...Object.entries(data).filter(([,value])=>String(value).trim()).map(([key,value])=>`${labels[key]}: ${value}`),'','Call: (402) 304-7059'].join('\n');result.hidden=false;result.focus();result.scrollIntoView({behavior:'smooth',block:'center'})});
copyButton.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(summary.textContent);copyStatus.textContent='Inquiry summary copied.'}catch{copyStatus.textContent='Select the summary above and copy it manually.'}});clearButton.addEventListener('click',()=>{if(!confirm('Clear the saved booking draft from this device?'))return;form.reset();localStorage.removeItem(draftKey);result.hidden=true;copyStatus.textContent='Saved draft cleared.'});
document.querySelector('.hero-copy').textContent='Booking artists for venues, festivals, and events. Call us when you need help finding the right fit.';document.querySelector('.booking-intro .eyebrow').textContent='Booking Details';document.querySelector('.booking-intro>p:not(.eyebrow)').textContent='Use this to organize the event details before you call. Nothing is sent to us. The draft stays in this browser until you clear it.';clearButton.textContent='Clear Saved Draft';document.querySelector('.form-note').textContent='This only creates a summary on your device. Copy it for your notes, then call us.';loadDraft();
const roster=document.querySelector('.roster');const artistTrack=roster.querySelector('.artist-track');const artistSlides=[...roster.querySelectorAll('.artist-slide')];const rosterDots=[...roster.querySelectorAll('.roster-dots button')];const dotRail=roster.querySelector('.roster-dots');const rosterViewport=roster.querySelector('.roster-viewport');const firstClone=artistSlides[0].cloneNode(true);const lastClone=artistSlides.at(-1).cloneNode(true);firstClone.setAttribute('aria-hidden','true');lastClone.setAttribute('aria-hidden','true');firstClone.inert=true;lastClone.inert=true;artistTrack.prepend(lastClone);artistTrack.append(firstClone);let artistIndex=0;let physicalIndex=1;let isSliding=false;let swipeStart=0;let swipePointer=null;
const sizeRoster=()=>{rosterViewport.style.height=`${artistSlides[artistIndex].offsetHeight}px`};const snapToPhysical=index=>{artistTrack.style.transition='none';physicalIndex=index;artistTrack.style.transform=`translateX(-${physicalIndex*100}%)`;void artistTrack.offsetWidth;artistTrack.style.transition=''};const showArtist=(index,instant=false)=>{const previous=artistIndex;if(!instant){const direction=index>=previous?-1:1;dotRail.animate([{transform:'translateX(0)',opacity:1,offset:0},{transform:`translateX(${direction*132}px)`,opacity:0,offset:.48},{transform:`translateX(${-direction*132}px)`,opacity:0,offset:.52},{transform:'translateX(0)',opacity:1,offset:1}],{duration:550,easing:'ease-in-out'})}artistIndex=(index+artistSlides.length)%artistSlides.length;let target=artistIndex+1;if(!instant&&previous===artistSlides.length-1&&index>=artistSlides.length)target=artistSlides.length+1;if(!instant&&previous===0&&index<0)target=0;physicalIndex=target;if(instant)artistTrack.style.transition='none';else isSliding=true;artistTrack.style.transform=`translateX(-${physicalIndex*100}%)`;artistSlides.forEach((slide,i)=>{const active=i===artistIndex;slide.setAttribute('aria-hidden',String(!active));slide.inert=!active});rosterDots.forEach(dot=>dot.setAttribute('aria-pressed','false'));requestAnimationFrame(sizeRoster);if(!instant)artistTrack.addEventListener('transitionend',()=>{if(target===0)snapToPhysical(artistSlides.length);if(target===artistSlides.length+1)snapToPhysical(1);isSliding=false},{once:true});if(instant){void artistTrack.offsetWidth;artistTrack.style.transition=''}};
let autoCycleTimer=0;let resumeCycleTimer=0;let holdUntil=0;const clearCycleTimers=()=>{clearTimeout(autoCycleTimer);clearTimeout(resumeCycleTimer)};const scheduleAutoCycle=()=>{clearCycleTimers();if(document.hidden||roster.contains(document.activeElement))return;const remaining=holdUntil-Date.now();if(remaining>0){resumeCycleTimer=setTimeout(scheduleAutoCycle,remaining);return}autoCycleTimer=setTimeout(()=>{if(!document.hidden&&!roster.contains(document.activeElement)&&!isSliding)showArtist(artistIndex+1);scheduleAutoCycle()},10000)};const holdArtist=()=>{holdUntil=Math.max(holdUntil,Date.now()+120000);scheduleAutoCycle()};const chooseArtist=index=>{if(isSliding)return;showArtist(index);holdArtist()};
roster.querySelector('.roster-prev').addEventListener('click',()=>chooseArtist(artistIndex-1));roster.querySelector('.roster-next').addEventListener('click',()=>chooseArtist(artistIndex+1));rosterDots.forEach((dot,i)=>dot.addEventListener('click',()=>chooseArtist(i)));roster.addEventListener('click',holdArtist);roster.addEventListener('focusin',()=>{clearCycleTimers()});roster.addEventListener('focusout',()=>setTimeout(()=>{if(!roster.contains(document.activeElement)){holdArtist()}},0));rosterViewport.addEventListener('pointerdown',event=>{if(event.target.closest('button,a')||!event.isPrimary||event.button!==0)return;swipePointer=event.pointerId;swipeStart=event.clientX;rosterViewport.setPointerCapture(event.pointerId)});rosterViewport.addEventListener('pointerup',event=>{if(event.pointerId!==swipePointer)return;const distance=event.clientX-swipeStart;swipePointer=null;if(rosterViewport.hasPointerCapture(event.pointerId))rosterViewport.releasePointerCapture(event.pointerId);if(Math.abs(distance)>55)chooseArtist(artistIndex+(distance<0?1:-1))});rosterViewport.addEventListener('pointercancel',event=>{if(event.pointerId===swipePointer)swipePointer=null});document.addEventListener('visibilitychange',()=>{if(document.hidden)clearCycleTimers();else scheduleAutoCycle()});artistSlides.forEach(slide=>{const link=slide.querySelector('.artist-booking');const artist=slide.getAttribute('aria-label');link.href='#book-talent';link.dataset.artist=artist;link.addEventListener('click',()=>{artistSelect.value=artist;saveDraft();setTimeout(()=>artistSelect.focus(),500)})});window.addEventListener('resize',sizeRoster,{passive:true});const lastStart=Number(sessionStorage.getItem('semperFiLastArtist'));const startChoices=artistSlides.map((_,index)=>index).filter(index=>index!==lastStart);const randomStart=startChoices[Math.floor(Math.random()*startChoices.length)];sessionStorage.setItem('semperFiLastArtist',String(randomStart));showArtist(randomStart,true);scheduleAutoCycle();
const bookingCall=document.createElement('a');bookingCall.className='button button-outline booking-call';bookingCall.href='tel:+14023047059';bookingCall.innerHTML='Call Semper Fi <span>→</span>';document.querySelector('.booking-intro').append(bookingCall);document.querySelector('#inquiry-details').removeAttribute('placeholder');document.querySelectorAll('#venues-promoters,#festivals-events').forEach(section=>section.remove());document.querySelectorAll('a[href="#venues-promoters"],a[href="#festivals-events"]').forEach(link=>link.remove());document.querySelectorAll('a[href="#about"]').forEach(link=>{link.href='#shows';link.childNodes[0].textContent='Shows '});const process=document.querySelector('.process');process.outerHTML='<section id="shows" class="section show-map"><div class="show-map-heading"><p class="eyebrow gold">Live Shows</p><h2>Where we’re playing.</h2><p>Dates and locations will appear here as they are added.</p></div><div class="artist-filters" aria-label="Filter map by artist"><label><input type="checkbox" data-artist-filter="anthony-segovia" checked><span></span>Anthony Segovia</label><label><input type="checkbox" data-artist-filter="the-traynr-band" checked><span></span>The Traynr Band</label><label><input type="checkbox" data-artist-filter="briella-steiner" checked><span></span>Briella Steiner</label></div><div class="map-layout"><div class="map-canvas" role="img" aria-label="Show location map"><div class="map-grid"></div><span class="map-label map-nebraska">NEBRASKA</span><span class="map-label map-kansas">KANSAS</span><span class="map-label map-dakota">SOUTH DAKOTA</span><div class="map-empty"><strong>No dates added yet</strong><span>Send the artist, date, venue, and city to place the first pin.</span></div></div><div class="show-list"><p class="eyebrow gold">Upcoming Dates</p><div class="empty-dates">Show announcements will be listed here.</div></div></div></section>';document.querySelectorAll('[data-artist-filter]').forEach(box=>box.addEventListener('change',()=>{const artist=box.dataset.artistFilter;document.querySelectorAll(`[data-map-artist="${artist}"]`).forEach(item=>item.hidden=!box.checked)}));
